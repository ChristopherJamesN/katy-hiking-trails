<p id="notice"><%= notice %></p>

<h1>Trails</h1>

<table class="table">
  <thead>
    <tr>
      <th>Name </th>
      <th>Reservoir </th>
      <th>Maplink </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trails.each do |trail| %>
      <tr>
        <td><%= link_to trail.name, trail %></td>
        <td><%= trail.reservoir %></td>
        <td><%= link_to "Link to Map", "#{trail.maplink}", :target => "_blank" %></td>
        <td><%= link_to 'Trail Detail', trail %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
